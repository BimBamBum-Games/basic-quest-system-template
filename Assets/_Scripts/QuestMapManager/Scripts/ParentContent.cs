using UnityEngine;

namespace JuniusGame.QuestMapManager.Scripts {
    public class ParentContent : MonoBehaviour {
        //This component atatched to UI parent content which is in scrollview which is content transform.
    }
}